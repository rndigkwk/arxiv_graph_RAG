# arXiv GraphRAG Streamlit 챗봇

`notebooks/arxiv_ai_kg_to_aura.ipynb`가 Aura에 적재한 arXiv 지식 그래프에 질문하는 Streamlit 앱입니다.
코드는 저장소 루트에 있고(`app.py`·`chatbot.py`·`gds_pagerank.py`·`metagraph.py`), 이 문서만 `app/`에 있습니다.

화면은 `홈` · `챗봇` · `PageRank 순위` 세 탭이고, 기능은 다음과 같습니다.

- **`translate_to_english`(한국어 → 영어 번역)** 도구를 넣었습니다.
  그래프의 제목·초록·저자 이름·분류 코드가 모두 영어라서, 한국어 질문은 검색 전에 번역해야 합니다.
- **답변은 기본이 영어**입니다. 답변 아래의 `🇰🇷 한국어로 번역` 버튼을 누르면 같은 답변의 한국어 번역으로
  바뀌고, `🇺🇸 영어 원문 보기`로 되돌아옵니다. 번역은 답변마다 따로 기억하고 한 번만 호출합니다.
- 답변이 특정 논문을 중심으로 하면 **개인화 PageRank**가 인용망에서 가까운 논문을 함께 찾아 줍니다.
  논문을 지목했을 때뿐 아니라 주제로 검색해 찾은 논문에 대해서도 돕니다.
- `PageRank 순위` 탭에서 **cs.AI 논문의 PageRank 순위**를 봅니다.
- `홈` 탭에서 **스키마와 커뮤니티 메타그래프**를 Plotly로 봅니다.

## 실행합니다

저장소 최상위에서 실행합니다. `.env`는 **저장소 루트**의 파일을 읽으므로 앱 폴더로 옮기지 않아도 됩니다.

```bash
uv run streamlit run app.py --server.fileWatcherType none
```

`--server.fileWatcherType none`은 이 환경의 `torch` 같은 패키지에서 나는 파일 감시 에러를 막습니다.
코드를 고친 뒤에는 앱을 다시 실행합니다.

## 연결 정보

저장소 루트 `.env`의 값을 `python-dotenv`로 읽습니다. 적재 노트북과 같은 키입니다.

| 환경변수 | 용도 |
|---|---|
| `NEO4J_URI` | Aura 연결 URI (`neo4j+s://…`) |
| `NEO4J_USER` 또는 `NEO4J_USERNAME` | 사용자명 |
| `NEO4J_PASSWORD` | 비밀번호 |
| `NEO4J_DATABASE` | 생략하면 `neo4j` |
| `OPENAI_API_KEY` | 임베딩·LLM API 키 |
| `OPENAI_MODEL` | 생략하면 `gpt-5.6-luna` |

질문 임베딩은 적재 때와 같은 `text-embedding-3-large` 768차원이고, 벡터 인덱스는
`arxiv_paper_embedding_index`입니다. 저장된 논문 벡터는 다시 만들지 않습니다.
준비 단계에서 인덱스가 `ONLINE`이 아니면 에러로 알리고 멈춥니다.

## 파일 네 개

| 파일 | 핵심 역할 |
|---|---|
| [chatbot.py](../chatbot.py) | `.env` → 연결·인덱스 확인 → 도구 4개 → 에이전트 → 검증한 답변 → PageRank |
| [app.py](../app.py) | 홈 탭, 챗봇 탭(대화·답변·번역 버튼·그래프·근거), PageRank 순위 탭 |
| [gds_pagerank.py](../gds_pagerank.py) | (선택) Aura Graph Analytics 세션으로 같은 PageRank 계산 |
| [metagraph.py](../metagraph.py) | 홈 탭의 Plotly 메타그래프(스키마·커뮤니티) |

`chatbot.py`는 함수를 정의한 뒤 마지막 준비 코드에서 연결·검색기·에이전트를 한 번 만들고,
`app.py`의 `@st.cache_resource`가 이 모듈을 세션 사이에 재사용합니다.

## 홈 탭

세 탭 중 맨 왼쪽입니다. 사이드바에 있던 스키마 설명을 여기로 옮기고, 그림으로 바꿨습니다.

**스키마 메타그래프** — 라벨 하나가 점 하나입니다. 점 크기와 선 굵기는 화면을 열 때 Aura에서
직접 센 실제 개수이고, 점·선에 마우스를 올리면 속성과 관계 방향이 나옵니다.
`REFERENCES`는 논문끼리의 관계라 자기참조 고리로 그립니다.
`ArxivPaper의 속성` 표에 속성별 타입과 뜻을 적어 두었습니다.

**커뮤니티 메타그래프** — [`notebooks/aura_graph_rag_quality_report.ipynb`](../notebooks/aura_graph_rag_quality_report.ipynb)의
`plot_community_metagraph`(matplotlib)를 Plotly로 옮긴 것입니다. Leiden 커뮤니티 하나가 점 하나,
점 크기는 논문 수, 선 굵기는 두 커뮤니티 사이의 상호 인용 수입니다. 배치 시드도 노트북과 같은 42라
그림이 실행마다 같습니다.

PageRank 순위는 `PageRank 순위` 탭에만 둡니다. 홈에서 겹쳐 보여 주지 않습니다.

커뮤니티 계산에는 GDS가 필요하므로 앱에서 다시 돌리지 않고, 그 노트북이 저장한
`reports/aura_graph_rag_quality_report.json`을 읽어 그립니다. 그 파일이 없으면 스키마 메타그래프만
나오고, 노트북을 실행하라는 안내가 뜹니다.

## 도구 네 개

| 도구 | 인수 | 하는 일 |
|---|---|---|
| `translate_to_english` | `text` | 한국어 등 비영어 입력을 영어 검색어로 번역합니다. 검색 근거가 아닙니다. |
| `search_graph` | `cypher` | 조회 전용 Cypher를 실행해 저자·인용·분류·기간 관계를 읽습니다. |
| `search_papers` | `query`, `top_k` | 제목+초록 임베딩으로 의미가 가까운 논문을 최대 10편 찾습니다. |
| `rank_related_papers` | `arxiv_ids`, `top_k` | 개인화 PageRank로 그 논문과 인용망에서 가까운 논문을 찾습니다. |

- `search_graph`는 `EXPLAIN`으로 실행 계획을 먼저 확인해 **조회 전용 쿼리만** 실행합니다.
  쓰기 쿼리는 거부하고, 문법 오류는 예외 대신 `error` 값으로 돌려주어 에이전트가 고쳐서 다시 부릅니다.
  그 시도는 `검색 과정과 근거 확인`에 남습니다.
- 매개변수(`$param`)는 쓰지 않습니다. 에이전트가 값을 Cypher 문자열 안에 직접 넣습니다.
- 분류는 노드가 아니라 속성입니다. 대표 분류는 `p.primary_category = "cs.AI"`(인덱스),
  부가 분류까지 포함하면 `"cs.AI" IN p.categories`(전체 훑기)입니다.

## 답변과 검증

LLM의 최종 출력은 두 필드입니다(`ProviderStrategy(GroundedAnswer, strict=True)`).

| 필드 | 내용 |
|---|---|
| `answer` | 검색 근거로 작성한 **영어** 답변 문자열 |
| `evidence_ids` | 답변에 사용한 `ArxivPaper.arxiv_id` 목록 |

`ask`는 `agent.stream(..., stream_mode=["messages", "values"])`로 생성 중인 답변을 받아 화면을 바로
갱신하고, 생성이 끝나면 **이번 질문의 호출 기록만** 모아 인용 ID를 검사합니다. 검색 결과에 없는 ID가
있으면 같은 근거로 한 번 수정하고 다시 검사합니다. 계속 실패하면 답변을 대화에 저장하지 않습니다.
ID가 존재하는지만 보는 검사이므로, 답변 내용은 근거 표와 초록으로 직접 대조하세요.

Cypher는 `answer_value`(답할 값)와 `evidence_ids`(그 값의 근거 `arxiv_id` 목록)를 반환해야 합니다.
논문을 특정할 수 없는 순수 집계라면 두 곳 모두 빈 목록이어도 됩니다.

## 챗봇 탭

질문 하나에 대해 화면이 위에서부터 이렇게 쌓입니다.

| 순서 | 내용 |
|---|---|
| 1 | 도구 선택 표시 — `한국어→영어 번역 / 벡터 의미 검색 / Text2Cypher / 개인화 PageRank`의 선택 여부. 실제 호출 기록에서 읽으므로 짐작이 아닙니다 |
| 2 | `검색어 번역: <원문> → <영어>` — 번역 도구를 썼을 때만 |
| 3 | 영어 답변 + `근거 arXiv ID` + 번역 버튼 |
| 4 | `검색된 그래프` — 아래 **그래프를 읽습니다** 참고 |
| 5 | 접이식 네 칸 — 해당 결과가 있을 때만 나옵니다 (아래 참고) |

### 접이식 네 칸

#### `검색 과정과 근거 확인` — 항상 나옵니다

- **답변의 인용 arXiv ID** — 답변 아래에 붙은 근거 ID를 목록으로 다시 보여 줍니다.
- **도구 요청 순서** — 실제 호출 기록을 번호 순으로 나열합니다. 이름은 한국어로 바꿔 적습니다
  (`한국어→영어 번역`, `Text2Cypher 관계 조회`, `벡터 의미 검색`, `개인화 PageRank`).
  도구마다 무엇을 넘겼는지도 함께 적습니다 — 번역은 `번역할 문장`, 벡터 검색은 `검색어`,
  개인화 PageRank는 `시드 논문`.
- **도구 응답** — 호출마다 성공·실패 상태와 도구가 돌려준 값 원본입니다.
- **실행한 Cypher** — Text2Cypher를 쓴 경우 쿼리 전문과 결과 행 표입니다.
- **Cypher 오류(에이전트가 다시 작성함)** — 에이전트가 문법이 틀린 쿼리를 썼다가 고쳐 부른
  경우 그 오류 메시지가 남습니다. 대화는 끊기지 않으므로, 여기서만 그 시도를 확인할 수 있습니다.

#### `개인화 PageRank로 찾은 관련 논문` — 관련 논문을 찾았을 때

표 한 장입니다: **순위 · PageRank · arXiv ID · 제목 · 대표 분류 · 피인용 · 게재일**.
PageRank 점수는 1e-4 수준이라 지수 표기로 보여 줍니다.
이 순위는 전체에서 유명한 논문이 아니라 **시드 논문 주변에서** 중요한 논문입니다.

#### `벡터로 찾은 논문의 초록` — 벡터 의미 검색을 썼을 때

찾은 논문마다 네 줄입니다.

1. `arXiv ID · 제목`
2. `Neo4j 벡터 유사도: 0.xxx (클수록 유사함) / 분류: cs.AI, cs.LG`
3. **초록 전문**
4. `arXiv PDF` 링크 버튼

#### `그래프에 그린 논문` — 그래프에 그린 논문이 있을 때

바로 위 `검색된 그래프`에 실제로 그려진 논문만 표로 정리합니다:
**arXiv ID · 제목 · 대표 분류 · 게재일 · 저자 수 · 피인용 · 인용 · 출처**.
`저자 수`는 그림에서 `저자 N명 더`로 접힌 수까지 포함한 전체이고,
`출처`는 `ai`(수집한 논문)인지 `reference`(인용으로 딸려온 논문)인지입니다.

## 그래프를 읽습니다

답변이 인용한 논문(없으면 이번에 검색한 논문)을 최대 15편까지 다시 조회해 그립니다.

- **실선 `REFERENCES`**: 그린 논문들 **사이에** 저장된 인용 관계입니다. 밖으로 나가는 인용은 그리지 않습니다.
- **점선 `AUTHORED`**: 저장된 저자 표기 연결입니다. 논문마다 3명까지 보이고, 나머지는 `저자 N명 더`로 묶습니다.
  논문이 6편을 넘으면 글자가 작아지지 않도록 저자를 생략하고 논문과 인용 관계만 그립니다.
- 그림은 검색한 근거의 범위를 보여 줍니다. 답변이 실제로 인용한 논문은 답변 아래의 arXiv ID로 확인합니다.
- `개인화 PageRank로 찾은 관련 논문`에서 시드 주변 순위를, `벡터로 찾은 논문의 초록`에서 유사도
  점수와 초록·PDF 링크를, `그래프에 그린 논문`에서 게재일·저자 수·피인용 수를 봅니다.

## PageRank

감쇠 계수 `0.85`, 최대 200회 반복, 대상은 `(:ArxivPaper)-[:REFERENCES]->(:ArxivPaper)` 인용망입니다.
계산 엔진은 두 가지이고 `PageRank 순위` 탭에서 고릅니다. **두 엔진의 순위는 같습니다.**

| | 직접 계산 (기본) | Aura GDS 세션 |
|---|---|---|
| 계산 위치 | 이 앱의 `scipy` 희소 행렬 | Aura Graph Analytics 세션 |
| 비용 | 없음 | **Free 등급은 청구 없음**, 유료 등급은 GB-분(최소 10분) |
| 처음 한 번 | 인용망 읽기 약 20초 | 세션 접속 약 8초 + 원격 투영 |
| 이후 한 번 | 0.1초 미만 | 3~5초 |
| 점수 눈금 | 합이 1이 되게 정규화 | GDS 기본값(정규화하지 않음) |
| 고립 논문 | 인용이 없는 논문도 포함 | 인용에 참여한 논문만 투영됨(34,896편) |

점수 숫자는 눈금이 달라 직접 비교하면 안 되고, **순위만 비교**하세요.
`cs.AI`(두 범위 모두)와 `cs.LO`에서 상위 5편이 완전히 일치하는 것을 확인했습니다.

### Aura GDS 세션 엔진

[`gds_pagerank.py`](../gds_pagerank.py)가 `.env`의 `CLIENT_ID`·`CLIENT_SECRET`으로 Aura API에
인증하고 `graphdatascience`의 세션에 붙습니다. 프로젝트(테넌트) id는 프로젝트가 하나면 API로
찾고, 여럿이면 `.env`의 `AURA_PROJECT_ID`로 지정합니다.

- **이미 Ready인 세션에만 자동으로 붙습니다.** 세션이 없으면 `새 GDS 세션 만들기` 버튼을 눌러야
  만들어집니다. TTL 30분 동안 쓰지 않으면 스스로 내려갑니다.
- 인용망은 `gds.graph.project.remote(s, t)`로 AuraDB에서 **원격 투영**합니다.
  이 투영은 REFERENCES에 참여한 논문만 담으므로 고립 논문 2,958편이 빠집니다.
- 세션이 돌려주는 `nodeId`는 **AuraDB의 내부 노드 id와 같습니다.** 그래서 순위에 오른 id만
  조회 전용 Cypher로 되읽어 제목·분류를 붙입니다. **DB에 `pagerank` 속성을 쓰지 않습니다.**
- 점수 높은 순으로 1000개씩 끊어 확인하므로, `cs.LO`(432편)처럼 상위권에 드문 분류도
  요청한 수만큼 채웁니다.
- 세션이 만료되면 에러를 표시하고 직접 계산으로 바꾸도록 안내합니다.

#### 요금

이 저장소의 인스턴스는 `free-db`(AuraDB Free)이고, 그러면 Graph Analytics도 **Free 등급이라
세션이 청구되지 않습니다.** 앱이 Aura API로 인스턴스 등급을 조회해 화면에 표시합니다.

| | Free 등급 | 유료 등급 |
|---|---|---|
| 요금 | 청구 없음 | GB-분, 최소 10분 (2GB를 25분 쓰면 50 GB-분) |
| 세션 메모리 | 최대 2GB | Pro·BC 128GB, VDC 512GB |
| 동시 세션 | 1개 | Pro 트라이얼 3개, 그 외 100개 |
| 세션 최대 수명 | 4시간 | Pro 트라이얼 1일, 그 외 7일 |

이 인용망은 Aura가 추정한 필요 메모리가 정확히 2GB라 Free 등급 한도에 들어갑니다.
데이터가 더 커져 4GB 이상이 필요해지면 Free 등급으로는 세션을 띄울 수 없습니다.
[Aura 과금 기준](https://neo4j.com/docs/aura/billing/billing-dimensions/#_aura_graph_analytics) ·
[등급별 한도](https://neo4j.com/docs/aura/graph-analytics/)

#### 그래도 직접 계산이 기본값인 이유

Free 등급에서 요금이 안 나오더라도 **GDS 세션에는 시간 제한이 있습니다.** Free 등급 세션은
최대 4시간이면 만료되고, TTL 30분 동안 놀아도 내려갑니다. 앱을 오래 켜 두면 GDS 엔진은 언젠가
반드시 끊기는데, 직접 계산은 세션 상태와 무관하게 항상 동작하고 두 번째 호출부터 0.1초 미만입니다.
동시 세션 1개 한도 때문에 다른 노트북이 세션을 쓰고 있으면 앱이 새로 띄우지도 못합니다.

그래서 **기본 엔진은 직접 계산으로 고정**합니다. GDS는 탭에서 명시적으로 골라야만 쓰이고,
고르기 전에는 `gds_pagerank` 모듈을 불러오지도 Aura API에 접속하지도 않습니다.
GDS 쪽이 유리해지는 것은 그래프가 훨씬 커지거나, PageRank 말고 커뮤니티 탐지·노드 임베딩처럼
직접 구현하기 번거로운 알고리즘을 쓸 때입니다.

### 개인화 PageRank (챗봇 탭)

답변이 특정 논문을 중심으로 할 때 에이전트가 `rank_related_papers`를 호출합니다.
사용자가 논문을 지목한 경우뿐 아니라, **주제로 검색해 찾은 논문에 대해서도** 상위 3편을 시드로
넣어 호출합니다. 순수 집계(편수·분류 전체 순위·저자 목록)에서는 호출하지 않습니다.
여기는 **항상 직접 계산**을 씁니다. 질문마다 도는 기능이라 세션 왕복 3~5초가 대화 응답에
그대로 붙기 때문입니다.

- 텔레포트를 **시드 논문에만** 몰아주므로, 전체에서 유명한 논문이 아니라 **그 논문 주변에서**
  중요한 논문이 위로 올라옵니다.
- 인용 방향을 그대로 쓰면 시드가 인용한 논문 쪽으로만 흘러가므로, 관련 논문을 찾을 때는
  인용한 쪽·인용된 쪽을 모두 이웃으로 보는 **무방향** 인용망을 씁니다.
- 결과는 저장된 인용 관계로 얻은 것이라 근거로 인용할 수 있고, `검색된 그래프`에도 함께 그립니다.
- 인용 관계가 저장되지 않은 논문은 결과가 비고, 그 사실을 답변에 적습니다.

예: `2303.11366`(Reflexion)으로 물으면 ReAct, Toolformer, Self-Refine, Voyager,
Generative Agents가 차례로 나옵니다. 분류 집계나 저자 질문에서는 호출하지 않습니다.

### cs.AI PageRank 순위 (PageRank 순위 탭)

이 페이지는 `cs.AI` 한 분류만 다룹니다. 분류 코드는 `app.py`의 `CATEGORY` 상수입니다.

| 설정 | 뜻 |
|---|---|
| 전체 인용망에서 계산한 뒤 cs.AI만 남기기 | 다른 분류에서 받은 인용도 점수에 들어갑니다. **전체에서의 중요도** |
| cs.AI 논문끼리의 인용만으로 계산하기 | 양 끝이 모두 cs.AI인 인용만 남깁니다. **이 분야 안에서의 중요도** |
| 부가 분류까지 포함 | 끄면 `primary_category`만(10,847편), 켜면 `categories` 기준(21,183편) |

두 방식 모두 1위는 ReAct(2210.03629)이지만 3위부터 갈립니다.
전체 기준에서는 Reflexion이, 분야 안 기준에서는 DeepSeekMath가 올라옵니다.
PageRank 순위는 피인용 순위와 다릅니다. 중요한 논문에게 인용받으면 피인용 수가 적어도 위로 갑니다.
표 아래에는 상위 논문들 **사이의** 인용 관계를 그래프로 그립니다.

직접 계산 엔진은 인용망을 처음 쓸 때만 읽으므로 탭에 `인용망 불러오고 PageRank 계산` 버튼을
두었습니다. 챗봇 탭을 쓰려고 앱을 열었을 때 20초를 기다리지 않게 하려는 것입니다.
GDS 엔진을 고르면 이 행렬을 아예 만들지 않고 필요한 수치를 Cypher로 셉니다.

## 질문을 바꿔 확인합니다

| 질문 | 확인할 동작 |
|---|---|
| cs.AI 논문 중에서 피인용이 가장 많은 논문 5편은? | 번역 + Text2Cypher, 생성 쿼리, 인용 그래프 |
| 지식 그래프 추론에 그래프 신경망을 쓰는 논문을 찾아 줘 | 번역 + 벡터 의미 검색, 초록·유사도 |
| Yoshua Bengio가 쓴 논문이 있어? | 저자 관계 조회. 이어서 `그 중 가장 최근 논문은?`으로 후속 질문 확인 |
| 대규모 언어 모델 에이전트를 다루는 cs.CL 논문을 알려 줘 | 벡터 검색 + Text2Cypher 조합. 벡터 인덱스에는 분류 필터가 없어 에이전트가 Cypher로 `primary_category = "cs.CL"`을 걸고 다시 검색합니다 |
| Attention Is All You Need를 인용한 논문을 알려 줘 | 인용 방향 조회(들어오는 REFERENCES) |
| 2026년에 나온 cs.AI 논문 중 피인용이 많은 3편은? | `published` 기간 필터 + 피인용 집계 |
| 2303.11366 논문과 관련된 논문 5편을 알려 줘 | 개인화 PageRank, 관련 논문 표, 인용 그래프 |

`대화 지우기`를 누르면 화면 기록·모델 기록·번역 상태·대기 중인 질문이 함께 비워집니다.

준비 에러는 루트 `.env`, Aura 인스턴스 상태, 적재 노트북 실행 여부를 확인합니다.
검색 에러는 OpenAI API 연결·사용량과 Aura 연결을 확인합니다.
빈 조회는 에러와 구분해 근거 부족으로 표시합니다.

## 알아 둘 점

- `ArxivAuthorName`은 **이름 표기 노드**입니다. 동명이인을 한 사람으로 묶으므로 저자 단위 결과에 주의하세요.
- 피인용 수와 PageRank 점수는 이 그래프에 적재된 `REFERENCES` 관계만 본 값입니다.
  arXiv 전체나 실제 인용 데이터베이스의 값이 아닙니다.
- 질문과 검색된 제목·초록은 모두 자료로 취급하며, 그 안의 지시문은 따르지 않습니다.
